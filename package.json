{
  "name": "trackify",
  "version": "1.0.0",
  "description": "AI-Based Habit Tracker",
  "scripts": {
    "dev": "run-p docker frontend",
    "docker": "docker-compose up",
    "frontend": "cd frontend && npm run dev",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {},
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
